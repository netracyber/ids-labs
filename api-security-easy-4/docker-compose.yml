version: '3.8'
services:
  api-security-easy-4:
    build: .
    container_name: api-security-easy-4
    ports:
      - "8073:80"
    volumes:
      - ./logs:/var/log/apache2
    restart: unless-stopped
    networks:
      - lab-network
      - tracking-network

networks:
  lab-network:
    driver: bridge
  tracking-network:
    external: true

version: '3.8'
services:
  api-security-easy-2:
    build: .
    container_name: api-security-easy-2
    ports:
      - "8071:80"
    volumes:
      - ./logs:/var/log/apache2
    restart: unless-stopped
    networks:
      - lab-network
      - tracking-network

networks:
  lab-network:
    driver: bridge
  tracking-network:
    external: true

version: '3.9'
services:
  nginx:
    image: nginx:latest
    container_name: ctf_nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    networks:
      - ctf_net

networks:
  ctf_net:
    external: true

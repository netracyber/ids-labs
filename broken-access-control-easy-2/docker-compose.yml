version: '3.8'
services:
  broken-access-control-easy-2:
    build: .
    container_name: broken-access-control-easy-2
    ports:
      - "8076:80"
    volumes:
      - ./logs:/var/log/apache2
    restart: unless-stopped
    networks:
      - lab-network
      - tracking-network

networks:
  lab-network:
    driver: bridge
  tracking-network:
    external: true
